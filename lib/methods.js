console.log("[SERVER] Loading methods.js");

Meteor.methods({
  calandra:function(argArray){
    //check(argString, String);
    // return 3;
    //res = argArray[1];
    console.log('sin comillas');
    //Actodes.insert({argArray});
  }
});
